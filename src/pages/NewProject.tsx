import React from 'react';
import NewProjectContainer from '@/components/project/NewProjectContainer';

const NewProject: React.FC = () => {
  return <NewProjectContainer />;
};

export default NewProject;

export default NewProject;